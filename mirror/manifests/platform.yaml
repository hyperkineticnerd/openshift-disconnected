apiVersion: mirror.openshift.io/v1alpha2
kind: ImageSetConfiguration
archiveSize: 2
storageConfig:
  local:
    path: ./metadata/platform/openshift
mirror:
  platform:
    graph: true
    channels:
      - name: stable-4.16
        type: ocp
        shortestPath: true
        minVersion: 4.16.14
        # maxVersion: 4.16.15
